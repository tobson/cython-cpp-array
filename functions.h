#ifndef MULTIPLY_H
#define MULTIPLY_H

#include <vector>

typedef std::vector<double> vector_t;

double multiply_scalars_cpp(const double&, const double&);
vector_t multiply_vectors_cpp(const vector_t&, const vector_t&);

#endif
